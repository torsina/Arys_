class APIRouter {
    constructor(data) {
        this.db = data.db;
        this.router = data.db;
    }
}
const router = require("express").Router(); // eslint-disable-line new-cap
module.exports = router;

//appeler le contexte